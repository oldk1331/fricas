sudo: false
dist: trusty
language: c
addons:
  apt:
    packages:
      - sbcl
compiler: gcc
script:
  - ./configure && make 2>&1 | grep '")compile"' | sed 's/ | DAASE.*//'
  - cd src/input && make | grep 'tee.*output'
  - ../scripts/test.sh
